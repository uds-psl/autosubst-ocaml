open Autosubst_lib


(* Use some server library
 * one http POST endpoint that takes the arguments for the program
 * and returns a zip file1 *)
(* let main () =
 *   let open ErrorM in
 *   match run (Program.main Sys.argv) with
 *     | Ok x -> print_endline x
 *     | Error x -> print_endline ("Error:\n" ^ x)
 *
 * let () = main () *)

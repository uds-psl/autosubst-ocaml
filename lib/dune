(library
  (name autosubst_lib)
  (preprocess (pps ppx_deriving.show ppx_deriving.ord ppx_fields_conv ppx_compare))
  (libraries base monadic stdio lwt.unix coq.vernac angstrom ocamlgraph)
  (flags (:standard -rectypes -w -27)))

(env
  (dev
    (flags (:standard -warn-error -A))))

(library
  (name autosubst_lib)
  (preprocess (pps ppx_deriving.show ppx_deriving.ord ppx_fields_conv ppx_variants_conv ppx_compare))
  (libraries base monadic coq.vernac )
  (flags (:standard -rectypes -w -27)))

(env
  (dev
    (flags (:standard -warn-error -A))))

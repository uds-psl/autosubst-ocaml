(library
  (name autosubst_lib)
  (preprocess (pps ppx_deriving.show))
  (libraries monadic coq.vernac coq.interp angstrom ocamlgraph)
  (flags (:standard -rectypes -w -27)))

(env
  (dev
    (flags (:standard -warn-error -A))))

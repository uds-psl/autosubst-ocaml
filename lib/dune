(library
  (name autosubst_lib)
  (preprocess (pps ppx_deriving.show ppx_deriving.ord ppx_fields_conv ppx_compare))
  (libraries core monadic coq.vernac)
  (flags (:standard -rectypes)))
(env
  (dev
    (flags (:standard -warn-error -A))))
